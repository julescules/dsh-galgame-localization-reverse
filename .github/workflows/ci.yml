name: ci

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Verify provider and zero-dependency utilities
        run: npm run check
      - name: Inspect the packed file allowlist
        run: npm pack --dry-run
      - name: Build release metadata
        shell: pwsh
        run: |
          $package = (npm pack --silent).Trim()
          node scripts/build-release-metadata.mjs $package builds/ci
          ./scripts/verify-release.ps1 -PackagePath $package -ChecksumsPath builds/ci/SHA256SUMS
